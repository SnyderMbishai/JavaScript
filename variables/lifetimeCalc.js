let currentAge = 23;
let maxAge= 80;
let amountPerDay = 3;

let remainingDays= (80-23)*365;
let neededSnacks = (remainingDays*3);
console.log(`You will need ${neededSnacks} packets of groundnuts to last you until the ripe old age of ${maxAge}`);